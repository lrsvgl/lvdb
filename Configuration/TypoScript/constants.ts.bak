
plugin.tx_lvdb_datenbank {
  view {
    # cat=plugin.tx_lvdb_datenbank/file; type=string; label=Path to template root (FE)
    templateRootPath =
    # cat=plugin.tx_lvdb_datenbank/file; type=string; label=Path to template partials (FE)
    partialRootPath =
    # cat=plugin.tx_lvdb_datenbank/file; type=string; label=Path to template layouts (FE)
    layoutRootPath =
  }
  persistence {
    # cat=plugin.tx_lvdb_datenbank//a; type=string; label=Default storage PID
    storagePid =
  }
  settings {
    # cat=plugin.tx_lvdb_datenbank//a; type=string; label=Default details page PID
    detailsPid =
    # cat=plugin.tx_lvdb_datenbank//a; type=string; label=Default category page PID
    categoryPid =
    # cat=plugin.tx_lvdb_datenbank//a; type=string; label=Debug
    debug =
    # cat=plugin.tx_lvdb_datenbank//a; type=string; label=Slider Data-Interval
    dataInterval = 4500
    # cat=plugin.tx_lvdb_datenbank//a; type=string; label=Google Maps Api
    googleApiLink = http://maps.google.com/maps/api/js?v=3.9
  }
}

plugin.tx_lvdb_navigation {
  view {
    # cat=plugin.tx_lvdb_navigation/file; type=string; label=Path to template root (FE)
    templateRootPath =
    # cat=plugin.tx_lvdb_navigation/file; type=string; label=Path to template partials (FE)
    partialRootPath =
    # cat=plugin.tx_lvdb_navigation/file; type=string; label=Path to template layouts (FE)
    layoutRootPath =
  }
  persistence {
    # cat=plugin.tx_lvdb_navigation//a; type=string; label=Default storage PID
    storagePid =
  }
  settings {
    # cat=plugin.tx_lvdb_navigation//a; type=string; label=Default details page PID
    detailsPid =
    # cat=plugin.tx_lvdb_navigation//a; type=string; label=Default category page PID
    categoryPid =
    # cat=plugin.tx_lvdb_navigation//a; type=string; label=Debug
    debug =
  }
}
